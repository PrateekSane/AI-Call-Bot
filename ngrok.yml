# ngrok.yml
version: "2"
authtoken: 1txLVBmIHFvvA1CSua982J2XHOC_7vMUGQ3A7UMN2riq1Zysn # Replace with your ngrok auth token

tunnels:
  web:
    proto: http
    addr: 3000 # Port of your web application (e.g., Flask app)
    inspect: true # Enable inspection of requests

  websocket:
    proto: http # Use 'ws' for WebSocket or 'wss' for secure WebSocket
    addr: 8100 # Port of your WebSocket server
    inspect: true # Enable inspection of requests
